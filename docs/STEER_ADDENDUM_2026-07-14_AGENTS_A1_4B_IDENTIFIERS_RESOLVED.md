# Steering addendum — official Agents-A1 4B identifiers resolved; admission remains blocked

Date: 2026-07-14

Status: **binding program-control addendum; no scientific capture authorized**.

This addendum incorporates `steer.md`, `CODEX_AUTOSTEER.md`, the M38E frozen
protocol, the amended M39 preregistration draft, and every existing sealed-data,
verifier, privacy, provenance, exact-set, cap-escalation, power, multiplicity,
retry, production, repository-hygiene, independence, and stop rule. Nothing here
changes an active M38E process, row, task, family, band, seed, cap, threshold,
verifier, eligibility rule, comparator gate, or production gate.

## Superseding external-artifact status

The prior artifact-admission addendum correctly recorded the official
`InternScience/Agents-A1@3be929bb3c7471469dc7dd98cc3c856fc23e99dd`
release announcement while the exact hosted artifact was not yet visible.

The official InternScience Hugging Face organization and official Agents-A1
collection now expose the following repositories:

- `InternScience/Agents-A1-4B`
- `InternScience/Agents-A1-4B-FP8`
- `InternScience/Agents-A1-4B-Q4_K_M-GGUF`
- `InternScience/Agents-A1-4B-Q8_0-GGUF`
- `InternScience/Agents-A1-4B-F16-GGUF`

The exact official repository/model identifiers are therefore established. The
program status is superseded from:

`official-release-announced / exact-artifact-unresolved`

to:

`official-identifiers-resolved / artifact-admission-incomplete`

This is a status correction only. It is not artifact admission and authorizes no
model download, execution, scientific capture, feature selection, Jacobian, VJP,
finite-difference probe, hidden-state capture, router trace, expert trace, fit,
calibration, or performance claim.

## Remaining admission blockers

The official collection currently labels the unquantized and FP8 repositories as
approximately 5B parameters while labeling the GGUF variants as approximately 4B.
That listing-level discrepancy is not to be reconciled by assumption. The exact
architecture and parameter accounting remain unresolved until primary files are
pinned and inspected.

Before admission, a separate committed admission record must still verify every
requirement in
`STEER_ADDENDUM_2026-07-14_AGENTS_A1_4B_ARTIFACT_ADMISSION.md`, including:

1. immutable repository revision or commit SHA for the exact research artifact;
2. complete file manifest and cryptographic identities for all files actually
   used;
3. model card, license, lineage, configuration, tokenizer, tokenizer
   configuration, generation configuration, chat template, and custom modeling
   code;
4. exact `model_type`, architecture class, dense-versus-MoE classification,
   parameter accounting, layer count, hidden size, intermediate size, vocabulary,
   context limit, and attention design;
5. routed and shared expert counts, top-k, router normalization, dispatch behavior,
   and residual/expert combination semantics, if MoE;
6. exact compatibility or documented incompatibility with the pinned 35B
   Agents-A1 tokenizer, chat template, telemetry definitions, and runtime;
7. supported execution path and pinned package/custom-code revisions;
8. output-disabled metadata inspection, deterministic tokenization fixtures, and
   finite-value load smoke performed without outcome-bearing study rows;
9. privacy, repository-safety, and secret-linked provenance checks.

A mutable `main` branch, collection entry, model-card parameter label, filename,
quantized sibling, or repository name remains insufficient. Missing,
contradictory, or inaccessible primary evidence produces
`artifact-admission-blocked`, not an inferred pass.

## Canonical artifact choice for future admission work

When admission becomes authorized by program order, the first candidate must be
the official unquantized repository:

`InternScience/Agents-A1-4B`

The FP8 and GGUF siblings may be admitted separately only for explicitly bounded
runtime or quantization studies. They may not substitute for the unquantized
artifact in Jacobian/VJP ground-truth comparisons, and measurements from different
precisions or formats may not be pooled to satisfy power, calibration,
approximation-error, rank-stability, or replication gates.

No floating branch may be used for a scientific run. The eventual admission record
must pin an immutable revision and record the complete identity of every loaded
file before any outcome-bearing execution.

## Program order remains unchanged

1. Continue and finalize M38E unchanged, or record its frozen blocked/negative
   outcome honestly.
2. Keep M39 as the forward-only, observation-only **35B** completed-error
   comparator. The 4B release does not authorize replacing the pinned M39 model,
   collecting M39 rows early, or using 4B outcomes to select M39 features, layers,
   thresholds, classifiers, task families, or stopping rules.
3. Advance beyond M39 only if a prespecified forward-only block clears every
   frozen M39 parity, provenance, privacy, power, calibration, minimum-effect,
   multiplicity, nuisance-increment, router-increment, ambiguity, independence,
   and locked-holdout gate.
4. After that gate, perform the complete 4B artifact-admission audit and commit its
   result.
5. Only an admitted, immutable artifact may enter a separately preregistered
   Jacobian/VJP/finite-difference approximation study.
6. Only after the smaller-model approximation gate passes may the program run the
   frozen one-sequence 35B backward high-memory technical smoke. That smoke carries
   no scientific claim.

## Current claim boundary

- The official Agents-A1 project announced the 4B release on 2026-07-14.
- The official hosted repository/model identifiers listed above are established.
- The exact immutable revision, complete file identity, architecture, parameter
  accounting, tokenizer/runtime parity, and research readiness are not yet
  established.
- The artifact remains unadmitted and all 4B scientific capture remains
  prohibited.
- No Agents-A1 4B Jacobian, VJP, approximation, router, hidden-state, expert, or
  error-prediction result exists in this program.
- M38E remains incomplete and has no finalized scientific outcome.
- M39 remains design-only and capture-prohibited.
- No safe truncation, early exit, semantic correctness, causal repair, routing
  intervention, activation steering, or production utility is established.
- The research program is not complete.
