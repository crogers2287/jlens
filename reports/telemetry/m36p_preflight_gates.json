{
 "schema_version": 1,
 "run_kind": "m36p_preflight_blocked",
 "checkpoint": "cyankiwi/Agents-A1-AWQ-INT4",
 "revision_pinned": "3e522d4e46438c782789b73c8ff4503e0edd037c",
 "outcome": "blocked",
 "blocker": {
  "kind": "loader_dependency_incompatibility",
  "summary": "transformers 5.13.0 qwen3_5_moe uses fused expert tensors (experts.gate_up_proj); the checkpoint stores 256 per-expert compressed-tensors modules per layer (weight_packed/scale/shape/zero_point, asymmetric INT4 group-32). The loader runs an unconditional DecompressExperts weight conversion that (1) fails with AssertionError: Asymmetric quant requires zero-point values although zero-points exist in the ckpt, and (2) decompresses INT4 to BF16 during load, which cannot fit the 44 GiB two-GPU gate by construction.",
  "evidence": [
   "DecompressExperts on tensors destined for model.layers.39.mlp.experts.gate_up_proj. Ckpt contains: 6",
   "AssertionError: Asymmetric quant requires zero-point values",
   "torch.OutOfMemoryError during conversion on GPU 1"
  ],
  "first_attempt_note": "initial load also hit an allocator-fragmentation OOM, fixed with PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True; the conversion failure above is the real blocker"
 },
 "environment": {
  "transformers": "5.13.0",
  "compressed_tensors": "0.17.1",
  "torch": "2.5.1+cu121",
  "gpus": "2x RTX 3090 24GiB"
 },
 "per_steer_options_for_operator": [
  "A (transformers version pair): no documented fix found for the DecompressExperts asymmetric per-expert path as of 2026-07; treat as speculative venv churn against the working M22-M35 stack",
  "B (vLLM >= 0.22): supports Qwen3.5 MoE with compressed-tensors AWQ Marlin MoE (modular WNA16), so the checkpoint should RUN compressed; router logits are not a first-class API but published MoE-routing studies collect them by patching the MoE block \u2014 a bounded source patch could expose real router telemetry, satisfying the steer clause; otherwise B degrades to the black-box arm",
  "C (different checkpoint identity): operator decision; no silent switch"
 ],
 "service_state_after": "GPUs freed (50/18 MiB); ollama up; llama-server processes present",
 "per_task_text_persisted_publicly": false,
 "privacy_check_status": "aggregate diagnosis only; no text/paths/weights",
 "research_note": "option facts from vLLM v0.22 release notes and 2026 MoE-routing literature; validated only as published claims, not yet reproduced locally"
}
