{
 "schema_version": 1,
 "run_kind": "m38e_technical_amendment",
 "steer": "32d5918 (replaces the e2d5b5e amendment)",
 "nature": "control-plane only; the active first attempt was never touched; no driver, task, row, manifest, threshold, verifier, sampling, cap, or gate changed",
 "corrections": {
  "owned_process_group": "a minimal launcher (chdir + setsid + exec) makes the recorded PID the exact owned PGID; stall signals go only to that PGID after re-proving its cmdline and cwd; host-global pgrep/pkill/name-matching is gone; the superseded bash supervisor now hard-refuses execution",
  "attempt_accounting": "a persisted private launch record (captured live from the running original attempt: PID==PGID, cmdline, cwd, python, vllm/torch versions, protocol env) counts the original launch as attempt one; the controller permits at most one additional launch and blocks permanently once the budget is spent",
  "single_writer": "an exclusive nonblocking flock keyed to the row-bound run id is acquired before preflight and held for the retry lifetime; liveness of any recorded driver for the run identity (exact cmdline+cwd proof, PID-reuse safe) blocks launch",
  "runtime_identity": "retry verifies the original python executable, vllm and torch versions, protocol environment variables, and byte-equal model pointers between control and execution checkouts against the private launch record; missing evidence blocks; immutable-source + exact-ledger preflight runs after the lock and immediately before engine construction",
  "crash_consistent_budget": "attempt two is durably consumed BEFORE process creation: the private record is atomically replaced (temp + os.replace + fsync of file and parent directory) in state reserved with a unique nonce; any attempt-two trace (reserved/launching/running/exited), including reserved-without-PID after a crash, blocks every later launch permanently and is never refunded; the record is atomically advanced to running with the owned PID/PGID only after the exec identity proof",
  "model_identity_bound_to_original": "the original launch record (captured live from attempt one) now carries the model-pointer sha256; the control pointer, execution pointer, AND the actual retry launch argument must all match that recorded digest \u2014 two current files agreeing is insufficient; missing original evidence blocks",
  "ambiguous_process_blocks": "proc identity distinguishes proven-nonexistent (dead) from unreadable/partial /proc evidence (AmbiguousProcess -> block); a recorded PID with unreadable identity can never be treated as dead",
  "protocol_env_schema": "a frozen five-key protocol environment schema is defined in code and mirrored in the private record (values or explicit nulls); the retry environment sets recorded values exactly and explicitly removes recorded absences so ambient values cannot leak; missing schema evidence blocks",
  "durable_process_ownership": "a barrier handshake closes the unguarded launch window: the child setsids then BLOCKS on an inherited pipe and can only exec the scientific driver after the controller durably writes a nonce-bound launching record with the exact PID/PGID/expected-command/exec-dir/source/launcher+preflight+environment digests, rereads and exact-validates it, and releases the barrier; parent death before release means pipe EOF and the child exits without exec and without ledger contact; exec-timeout and post-creation exceptions terminate and reap only the exact owned group (or record a permanent ambiguous block when ownership cannot be proved) and durably record the terminal state \u2014 no failure path leaves a live unrecorded child",
  "lock_survival": "the run-ID flock descriptor is inherited through exec, so a controller crash after barrier release leaves the lock held by the surviving driver; the lock releases only when the driver exits",
  "pinned_control_artifacts": "the launcher and preflight bytes are pinned by sha256 in the private record and reverified immediately before process creation; any control-plane edit blocks a retry even when the immutable scientific checkout and ledger are unchanged",
  "complete_environment_envelope": "attempt one's COMPLETE process environment (77 variables) was captured privately as exact bytes with a digest while its /proc identity was proven; a retry environment is reconstructed solely from those recorded bytes \u2014 ambient os.environ is never inherited; missing, unreadable, or digest-mismatched evidence blocks",
  "no_explicit_unlock": "the controller never calls LOCK_UN after the lock descriptor is inherited; it closes only its own duplicate, so a surviving child keeps the run identity locked until its last descriptor closes (source-asserted by test)",
  "strict_termination_only": "permissive termination is gone: full ownership evidence (PID/PGID/SID, /proc stat start time, command phase, cwd) is captured while the child is behind the barrier and durably bound; ownership is re-proved immediately before BOTH the TERM and KILL signals with pidfd PID-reuse resistance where supported; any mismatch or unreadable field is a permanent ambiguous block with no signal",
  "fd_allowlist": "launches use close_fds=True with an exact pass-fd allowlist (barrier + run lock); the guarded launcher audits /proc/self/fd pre-exec and aborts (rc 4, no exec, no ledger contact) on any unexpected descriptor; the barrier closes before exec",
  "probes_under_original_identity": "the complete recorded environment is reconstructed and digest-verified BEFORE any package-dependent check; vllm/torch probes run under the original executable (bound by the sha256 captured from /proc/<pid>/exe while attempt one was alive) and the recorded environment, comparing version and module origin; the controller's ambient interpreter orchestrates file I/O only",
  "untracked_import_audit": "a read-only audit of attempt one's execution tree found 6 untracked paths, zero importable or executable by the driver, zero symlinks in import roots (aggregate PASS recorded privately); retry preflights and finalization require the all-untracked audit with an explicit private non-importable allowlist",
  "preflight_under_bound_identity": "the retry order is: load+validate record under lock \u2192 reconstruct+digest-verify the complete environment \u2192 bind the executable identity \u2192 run the Python-importing preflight AND all package probes under that exact executable and environment (never sys.executable/ambient) \u2192 reverify source/artifacts/model/ledger/executable immediately before reservation and process creation",
  "executable_binding": "the executable is bound by canonical path, device, inode, mode, size, and sha256 captured read-only while attempt one was alive; the invoked pathname must resolve to that exact identity; the preflight, probes, guarded launcher, and driver are all invoked by the bound executable; the launcher re-proves the sha behind the barrier immediately before scientific exec (aborts rc 5 on any change)",
  "package_origin_comparison": "vllm/torch (and locally imported packages) are compared on version AND canonical origin AND origin-file sha256 under the bound executable and recorded environment \u2014 same-version/different-origin resolution blocks (test-proven)",
  "fail_closed_liveness": "driver_alive is fail-closed: proven nonexistence or a different /proc start time (PID reuse) is dead; same start time with any cmdline/cwd/SID/PGID/phase mismatch, unreadable evidence, or missing recorded fields raises AmbiguousProcess and blocks; only exact full identity counts as alive",
  "reusable_untracked_audit": "scripts/m38e_untracked_audit.py enumerates all untracked files/dirs/symlinks, derives import+execution roots from the recorded environment and bound executable, and blocks importable/executable/symlinked/.pth/startup-hook/editable-install artifacts (only the explicit private operational allowlist outside roots passes); run by both retry preflight and finalization; attempt-1 tree audited clean",
  "pidfd_termination": "termination signals the leader through its pidfd (PID-reuse immune) and each session descendant only through its own freshly opened pidfd after per-process proof \u2014 never a bare killpg after leader identity is unavailable; platforms without pidfd_send_signal fail closed (ambiguous, no auto-termination)",
  "ambiguous_stall_no_hang": "an ambiguous stall termination durably records a permanent 'ambiguous' block, does NOT call an unbounded wait, closes only the controller's lock duplicate (leaving a surviving child its inherited lock), and returns blocked for private operator review"
 },
 "tests": "tests/test_m38e_retry_controller.py \u2014 31 synthetic tests adding executable file-identity binding, same-version/different-origin blocking, fail-closed liveness (same-start-mismatch ambiguous, PID-reuse dead), the reusable untracked-import audit (clean pass, importable block), pidfd termination, and the ambiguous-stall no-hang structural proof, alongside all prior barrier/lock/budget/FD/ownership proofs",
 "retry_launched": false,
 "privacy_check_status": "aggregate amendment only; PIDs/paths/env stay in the private record"
}
