{
 "schema_version": 1,
 "run_kind": "m38e_technical_amendment",
 "steer": "0e812c1 (replaces the 4cb5caa amendment)",
 "nature": "control-plane only; the active first attempt was never touched; no driver, task, row, manifest, threshold, verifier, sampling, cap, or gate changed",
 "corrections": {
  "fail_closed_retry_gate": "automatic retry is now permanently BLOCKED on this runtime: it requires an fd-bound execveat/fexecve exec path with a proven replacement-race boundary (the guarded launcher performs pathname os.execv, so the final-lookup race is not proven closed) AND a delegated identity-bound cgroup-v2/systemd kill scope for complete post-GO process-tree termination \u2014 neither is available in this pure-Python unprivileged environment. The steer authorizes failing closed; a retry now requires private operator review rather than auto-launch. This is the honest, safe default for a contingency this critical.",
  "git_slot_wiring_fixed": "the real bug is fixed: the audit's 4-arg boundary is <checkout> <env> <python-exe> <git-exe>; the controller now binds the git executable by canonical path + digest from attempt one and passes THAT (never the Python executable, never PATH); an integration test proves clean PASS, planted-shadow BLOCK, and that wiring python into the git slot fails closed.",
  "retained": "all prior handshake/liveness/ownership/audit/lock corrections from steers 3f422ad..4cb5caa remain in force; the fail-closed gate sits in front of them so none can be reached by an automatic retry."
 },
 "tests": "tests/test_m38e_retry_controller.py \u2014 34 synthetic tests including the fail-closed retry gate, bound-git identity, and the audit git-slot integration (clean pass / shadow block / wrong-wire fail-closed)",
 "retry_launched": false,
 "privacy_check_status": "aggregate amendment only; PIDs/paths/env stay in the private record",
 "honest_scope_correction": "the prior 'complete dependency closure' claim is downgraded to a NAMED-MODULE ORIGIN SAMPLE (11 modules) \u2014 it does not bind distribution manifests, native libraries, .pth effects, editable installs, entry points, or the full import graph, and is not a complete closure",
 "retry_status": "permanently fail-closed pending operator review; no automatic retry"
}
